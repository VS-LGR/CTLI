/**
 * Duração de um loop completo do GIF do hero (ms), derivada dos delays dos frames.
 * Se trocar `CTLI INTRo.gif`, atualize com: `node scripts/print-gif-loop-ms.mjs`
 */
export const HERO_INTRO_GIF_LOOP_MS = 3000;
