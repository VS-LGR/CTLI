/**
 * Duração de uma reprodução completa do GIF do hero (ms), soma dos delays dos frames.
 * GIF com loop único no ficheiro: coincide com a duração total da animação.
 * Se trocar `ctli_intro.gif`, atualize com: `node scripts/print-gif-loop-ms.mjs`
 */
export const HERO_INTRO_GIF_LOOP_MS = 20;
